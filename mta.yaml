_schema-version: "2.0.0"
ID: "com.sap.1084565.openui5-masterdetail-app"
version: 0.1.0

parameters:
  hcp-deployer-version: "1.0.0"

modules:
  - name: "ui5-sample-app"
    type: html5
    path: .
    parameters:
       version: 0.1.0-${timestamp}
    build-parameters:
       builder: grunt
       build-result: dist 
